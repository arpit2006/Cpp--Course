#include<iostream> //This is a preprocessor directive that includes the iostream (input-output stream) library.
using namespace std;//This tells the compiler to use the standard namespace, so you don't need to prefix standard library objects with std::.
int main()//his is the main function where the program starts execution. Every C++ program must have a main function
{
    cout << "Hello World..!"; //console outpu

    return 0; //Returning 0 from main signals to the operating system that the program completed without errors.
}